#include <bits/stdc++.h>
#include <math.h>
using namespace std;
int main()
{
    int x,n;
    double s;
    cin >> x >> n;
    cout <<fixed<<setprecision(10)<< (double)(pow(x,(double)1/n));
    return 0;
}
